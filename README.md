How to use

~~~
Usage: swagger-validator-cli [options] <filename>

swagger validator

Arguments:
  filename    file to validate

Options:
  --yaml      Input data is yaml. Default is json
  --yamlout   Use output format as yaml. Default is json
  -h, --help  display help for command
~~~
